use strict;
use warnings;
use Test::More;


use Catalyst::Test 'Veduta';
use Veduta::Controller::Geocoder;

#ok( request('/geocoder')->is_success, 'Request should succeed' );
ok(1,1);
done_testing();
